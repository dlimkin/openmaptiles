layer:
  id: "address"
  requires:
    layers:
      - housenumber
  description: |
      Address search
  buffer_size: 4
schema:
  - ./functions.sql
  - ./address.sql
datasources:
  - type: imposm3
    mapping_file: ./mapping.yaml